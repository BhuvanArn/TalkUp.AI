meta {
  name: updateApplicationStatus
  type: http
  seq: 3
}

patch {
  url: {{host}}/applications/{{applicationId}}
  body: json
  auth: inherit
}

body:json {
  {
    "status": "interview"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
